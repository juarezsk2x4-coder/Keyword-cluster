# Keyword-cluster
SEO app

## Install

`graphify` ships as the `graphifyy` package on PyPI. Recommended:

```bash
uv tool install graphifyy   # puts graphify on PATH automatically
```

See [INSTALL.md](INSTALL.md) for `pipx`/`pip` alternatives and PATH notes.
